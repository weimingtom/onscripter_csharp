		        //FIXME:???operator=
		        public ArrayVariable assign(ArrayVariable av){
----------

check all files, = to new CharPtr() 
check all return CharPtr function, return new CharPtr()

----------
xx f(CharPtr x)
{
	x = new CharPtr(x)
	...
}

